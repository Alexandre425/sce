
#include "comunication.h"

void readMessage (void)
{   
    
    unsigned char msg_cmd;
    unsigned char msg_data;
    
    
    switch(msg_cmd)
        case(RCLK):
            break;
        case(SCLK):
            break;
        case(RTL):
            break;
        case(RPAR):
            break;
        case(MMP):
            break;
        case(MTA):
            break;
        case(RALA):
            break;
        case(DAC):
            break;
        case(DATL):
            break;
        case(AALA):
            break;
        case(IREG):
            break;
        case(TRGC):
            break;
        case(TRGI):
            break;
        case(NMFL):
            break;
        default:
            break;
    
}